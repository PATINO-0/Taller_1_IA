# Solo para desarrollo con FastAPI en el puerto 8000.
# En Vercel, no configure esta variable: el frontend usará /api
# en el mismo dominio del despliegue.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

